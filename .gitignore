# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# === Spendly secrets — never commit (CLAUDE.md §8) ===
# Firebase config (added in Phase 5) — kept local; regenerate with
# `flutterfire configure` (see FIREBASE_SETUP.md)
**/google-services.json
**/GoogleService-Info.plist
firebase_options.dart
.firebaserc
firebase.json
# Environment / API keys (Gemini key added in Phase 7)
.env
.env.*
*.env
# Signing material
*.keystore
*.jks
key.properties
android/key.properties
# Generic local key / secret files
**/secrets.dart
**/*.pem
**/*.p12
service-account*.json
# === end secrets ===

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release
