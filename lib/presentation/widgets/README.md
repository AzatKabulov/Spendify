# `presentation/widgets/`

Reusable presentational widgets shared across screens: money text, category chips, budget warning banners, charts (`fl_chart` wrappers), reward animations, empty/loading/error states.

Keep them dumb — inputs and callbacks in, pixels out. No repository access, no Riverpod `ref` reaching past what's passed in.
