<?xml version="1.0" encoding="utf-8"?>
<!--
  Phase 10 security audit: HTTPS only, no cleartext anywhere.

  Every network call Spendify makes is HTTPS:
    - Google Gemini  (generativelanguage.googleapis.com)
    - Firebase Auth / Cloud Firestore  (Google SDK endpoints)
    - connectivity_plus reachability probe

  `cleartextTrafficPermitted="false"` makes any plaintext HTTP request fail
  fast rather than silently downgrade. Belt-and-suspenders with
  android:usesCleartextTraffic="false" in the manifest.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
